apiVersion: v1
kind: ConfigMap
metadata:
  name: settleup-config
data:
  SPRING_PROFILES_ACTIVE: docker
  POSTGRES_HOST: postgres
  POSTGRES_DB: settleup
  POSTGRES_USER: settleup
  PLAID_ENV: sandbox
  FRONTEND_ORIGIN: http://localhost:5173
