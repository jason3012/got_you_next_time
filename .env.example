# Local PostgreSQL (used from Phase 1)
POSTGRES_USER=settleup
POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_DB=settleup

# Plaid Sandbox (configured in Phase 6)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
PLAID_WEBHOOK_URL=

# Phase 5 authentication. Generate before running the backend:
# openssl rand -base64 48
JWT_SECRET=
# openssl rand -base64 32
TOKEN_ENCRYPTION_KEY=
